//set const array of numbers for input 
//use .length method starting at -1 to count backwards
//console.log result of countdown.

const numstring = [1, 2, 3];
for(let index = numstring.length - 1; index >= 0; index--) {
    console.log(numstring[index]);
};

const numstring1 = [1, 3, 5, 7, 9, 11];
for(let index = numstring1.length - 1; index >= 0; index--) {
    console.log(numstring1[index]);
};